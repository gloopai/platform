// Package base holds payment channel (PSP) contracts: [ChannelDriver], [BaseChannelDriver]
// (embeddable defaults), [ChannelBase], DTOs, and helpers.
// Import path: github.com/gloopai/pay/channeldriver/base.
//
// The root package channeldriver re-exports these symbols for convenience; new channel
// implementations may depend only on base + their own subpackage.
package base
