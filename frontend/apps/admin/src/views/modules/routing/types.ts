export type RoutingSummary = {
  algorithm_key: string
  algorithm_label: string
  enabled_pay_products: number
  enabled_channels: number
  active_bindings: number
  merchants_with_whitelist: number
  fused_channels: number
}
