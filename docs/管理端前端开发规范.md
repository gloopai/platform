# 管理端前端开发规范（`frontend/apps/admin`）

与 [`admin-platform.md`](./admin-platform.md) 配合使用，后续迭代请遵守本节约定。

---

## 1. 后台接口请求

- **统一入口**：所有需携带 **`X-Admin-Token`** 的接口必须经 `src/lib/adminApi.ts` 中的 **`adminRequest`**（或由其实现的 **`adminGet` / `adminPost` / `adminPut` / `adminDelete`**）发起。
- **禁止**：在业务页面或组件里手写 `fetch` 并重复拼鉴权头（除非该接口设计上不需要 Token，例如 **`POST /v1/admin/login`**，可单独 `fetch`）。
- **扩展**：若需额外 Header，使用 `adminRequest(path, { headers: { ... } })`，避免复制整段 `fetch` 样板代码。

---

## 2. 页面与模块目录

- **按业务模块建目录**：在 `src/views/modules/<模块名>/` 下放该模块的页面入口与子组件，例如 `modules/pay-products/`、`modules/channels/`、`modules/routing/`、`modules/stats/`。
- **路由**：`src/router.ts` 仍集中注册路由；页面组件可指向 `views/modules/.../XxxPage.vue`。
- **拆分原则**：单文件只负责拼装与数据流（请求、状态、子组件协调）；展示块拆成子组件（列表、表单、表格、工具栏等），**类型**可放在同目录 `types.ts`。
- **遗留页面**：`views/pages/` 下尚未迁移的页面可在后续重构时按同样规则迁入 `views/modules/`，不必一次性改完。

---

## 3. 长列表与大量选项（产品 / 通道等）

- **侧栏列表**（如支付产品列表、通道列表）：提供 **搜索框**（按 ID、名称、类型等客户端过滤）；列表区域 **固定最大高度 + 内部滚动**（如 `max-h-[min(70vh,560px)]`），避免整页被撑得很长。
- **上百条通道的下拉选择**：不要用原生 `<select>` 渲染全部 option；使用 **可搜索 + 滚动面板**（如 `components/ChannelPicker.vue`），未输入时可 **限制首屏条数**、提示用户输入关键字缩小范围；**已绑定/需排除的 ID** 通过 props 传入过滤。
- **表格**：行数多时对表格外层加 **max-height + overflow-auto**，表头可用 `sticky` 便于对照。

---

## 4. 统一交互组件（Toast / Tips / Dialog）

> 本节不仅适用于管理端，`frontend/apps/*` 新模块开发均按此约定执行。

- **统一来源**：交互组件统一来自 `frontend/shared/ui/`，禁止各业务模块重复实现 Toast、Tooltip(Tips)、Dialog（含可插槽内容弹窗）、Drawer。
- **目录约定**：按组件拆分在 `shared/ui/toast/`、`shared/ui/tooltip/`、`shared/ui/dialog/`、`shared/ui/drawer/`；公共导出入口为 `shared/ui/index.ts`。
- **Host 挂载**：每个 app 仅在布局根（`App.vue` 或 `Layout.vue`）挂载一次 `<UiToastHost />` 与 `<UiDialogHost />`，业务页面不得重复挂载。
- **组合式调用**：业务代码通过 `useUiToast()`、`useUiDialog()` 调用，优先从各 app 的 `src/composables/ui.ts`（或 `shared/ui`）导入。
- **禁止系统弹窗**：业务代码中禁止使用浏览器原生 `window.alert` / `alert()` / `window.confirm` / `confirm()`；确认与提示统一使用 `dialog.confirm/danger/alert`。
- **删除必须二次确认**：所有“删除/移除/清退/解绑”等破坏性操作，点击按钮后必须先弹 `dialog.confirm`（或 `dialog.danger`）确认，再执行请求；禁止直接执行删除。
- **服务端错误反馈**：接口返回错误（`catch` 到的后端报错）必须优先使用 `toast.error(...)` 给出明确提示；页面内联 `error` 文案可保留作辅助展示，但不能只写内联不弹提示。
- **操作结果提示（Tips）**：创建、编辑保存、删除、启停等操作完成后，统一给出轻提示（`toast.success/info`），避免用户无感知。
- **使用场景**：
  - `toast.success/error/info`：保存、创建、删除、提交等短反馈。
  - `dialog.confirm/danger/alert`：删除、停用、清退等需二次确认动作（文本确认）。
  - `UiDialog`：需要自定义内容区域（表单、说明块、自定义按钮区）的弹窗。
  - `UiTooltip`：字段说明、状态解释、图标提示等轻量提示。
- **体验约束**：同一操作不重复弹多个提示；失败提示与表单内联错误避免冲突（必要时保留一种主反馈 + 一种辅助反馈）。

### 4.2 编辑抽屉交互规范（管理台列表页）

- **编辑入口统一**：列表中的“编辑”按钮统一打开**右侧划入抽屉**（`UiDrawer`），不再使用列表行内编辑、页内卡片编辑、居中弹窗编辑作为默认形态。
- **按钮区位置**：抽屉操作按钮统一放在**抽屉左下角 footer 区域**，至少包含 `确认`（保存）与 `关闭`；业务需要可增加 `重置`、`删除` 等功能按钮。
- **保存成功行为**：抽屉内保存成功后，应**自动关闭抽屉**，并通过 `toast.success(...)` 提示成功。
- **保存失败行为**：保存失败时应**保持抽屉不关闭**，并通过 `toast.error(...)` 提示失败原因，便于用户继续修正后重试。
- **适用范围**：适用于管理台中所有“列表 -> 编辑详情”的交互；新需求必须遵守，存量页面按迭代逐步收敛。
- **当前例外（已确认）**：RBAC 模块（`modules/rbac/**`）现有行内编辑暂不纳入本条约束，按模块既有交互继续维护；后续如需统一改造再单独立项。

### 4.1 参考示例

```ts
import { useUiToast, useUiDialog } from '@/composables/ui'

const toast = useUiToast()
const dialog = useUiDialog()

toast.success('保存成功')

const ok = await dialog.confirm('确定要删除这条记录吗？')
if (ok) {
  // ...
}
```

---

## 5. 全局展示配置与服务器时间

- **配置入口**：全局国家/货币配置统一在管理台 `系统管理` 页面维护，接口为 `GET /v1/admin/display_settings` 与 `PUT /v1/admin/display_settings`。
- **金额展示**：管理台金额文案必须读取全局配置（至少包含 `currency_symbol` 与 `currency_code`），不允许新增硬编码 `¥`/`CNY`。
- **服务器时间**：管理台布局层显示服务器时间（基于 `/health` 的 `timestamp_ms` 校准），用于排查时区问题。
- **时间格式**：页面中所有时间展示继续使用 24 小时格式（`YYYY/MM/DD HH:mm:ss`）。

---

## 6. 新模块必须同步产出菜单/权限 SQL

- **强制要求**：新增管理台模块（页面、菜单、路由、按钮操作）时，必须同步提供 RBAC 数据变更 SQL，至少覆盖：
  - `rbac_admin_menu`（菜单项：侧栏/头像菜单）
  - `rbac_permission`（权限点）
  - `rbac_api_rule`（接口到权限点映射）
  - 必要的角色初始化授权（如测试/默认角色）
- **交付内容**：PR 或任务交付中必须包含“可执行 SQL 脚本 + 执行说明”，不能只改前端代码。
- **幂等约束**：SQL 应尽量可重复执行（`INSERT ... WHERE NOT EXISTS` / `ON CONFLICT DO NOTHING` / `ON DUPLICATE KEY` 等），避免重复部署报错。
- **变更顺序**：建议顺序为“菜单 -> 权限点 -> API 规则 -> 角色授权”，并保证外键/引用关系一致。
- **回滚意识**：涉及线上权限调整时，需提供回滚脚本或回滚步骤（至少说明删除/恢复哪些 `menu_key`、`perm_key`、规则与角色授权）。

## 6. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-03-22 | 初稿：`adminRequest` 统一鉴权；支付产品模块目录与组件拆分示例。 |
| 2026-03-22 | 长列表：搜索 + 滚动；`ChannelPicker` 大列表通道选择。 |
| 2026-03-23 | 补充 `modules/routing/`、`modules/stats/` 示例。 |
| 2026-03-23 | 全局 Toast：`useAdminToast` + `AdminToastHost`。 |
| 2026-03-23 | 增加全局国家/货币配置约定；管理台与商户台统一显示服务器时间。 |
| 2026-03-26 | 统一交互组件规范升级为 `shared/ui`：Toast/Tips/Dialog 全应用共用。 |
| 2026-03-26 | 新增约束：禁止原生 `alert/confirm`，统一改用 `useUiDialog`。 |
| 2026-03-26 | 新增约束：服务端错误走 `toast.error`；保存/删除等操作完成需给 `toast` 轻提示。 |
| 2026-03-26 | 新增约束：所有删除/移除类操作必须二次确认后执行。 |
| 2026-03-26 | 新增编辑交互规范：列表编辑统一右侧抽屉；按钮区放抽屉左下；保存成功自动关闭并 toast 成功，失败不关闭并 toast 失败。 |
| 2026-03-26 | 补充适用范围：RBAC（`modules/rbac/**`）现有行内编辑暂不纳入“统一右侧抽屉编辑”约束。 |
| 2026-03-26 | 新增 `shared/ui/drawer/UiDrawer.vue`，管理台抽屉能力统一沉淀到 shared/ui。 |
| 2026-03-26 | 新增 `shared/ui/dialog/UiDialog.vue`（可插槽内容弹窗），商户管理“划转”改为复用该组件。 |
| 2026-03-26 | 新增新模块交付要求：前端功能上线必须同步提供菜单/权限/RBAC 变更 SQL 与执行说明。 |
| 2026-03-26 | 结算与提现 phase2：新增 `merchant_withdrawals` 表与 RBAC SQL（`rbac_settlement_withdrawals.sql`）作为接口接入基础。 |
| 2026-03-26 | 结算与提现 phase2 继续：提现申请页接入真实 GET/POST 接口，先落地“创建即扣款成功”闭环，审核/打款在下一步扩展。 |
| 2026-03-26 | 结算与提现 phase2 补齐：提现改为 USDT 手动流程（申请→审核扣款→线下打款→确认完成），并接入 GET/POST/PUT(review)/PUT(payout) 全接口。 |
